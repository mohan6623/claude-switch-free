# AGENT_RULES.md

## Project Notes

- Follow the repository's `AGENTS.md` instructions for build, lint, test, imports, TypeScript strictness, and Bun-based workflows.
- Do not assume generated artifacts such as `dist/` or dependency folders such as `node_modules/` survived disk copies or formatting. Verify them directly when diagnosing command-shim failures.
